{
  // jsconfig does not work properly with CLI `tsc`, that is why using tsconfig.
  // Namely, tsc starts looking into node_modules which are imported
  // https://github.com/microsoft/TypeScript/issues/50862
  // https://github.com/microsoft/TypeScript/issues/43043
  // https://github.com/microsoft/TypeScript/issues/41350
  // https://github.com/microsoft/TypeScript/issues/6671
  "compilerOptions": {
    // Language and Environment

    "target": "ES2021",
    "lib": ["ES2021"],

    // Modules

    // We need to specify it because otherwise we face an error:
    // Dynamic imports are only supported when the '--module' flag is set to 'es2020', 'es2022', 'esnext', 'commonjs', 'amd', 'system', 'umd', 'node16', or 'nodenext'.ts(1323)
    "module": "CommonJS",

    // Completeness

    "skipLibCheck": true,

    // Emit

    "noEmit": true,

    // JavaScript Support

    "allowJs": true,
    "checkJs": true
  },
  "include": ["plugins/**/*.js", "src/**/*.11tydata.js", "src/_data/**/*.js"],
  "exclude": ["node_modules", "**/node_modules/*", "dist"]
}
